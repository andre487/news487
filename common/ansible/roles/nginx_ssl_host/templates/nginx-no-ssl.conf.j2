# Config is autogenerated – do not change it manually
server {
    listen 80;
    listen [::]:80;

    server_name {{ service_host }};

    location / {
        root /var/www/{{ service_host }};
    }
}
